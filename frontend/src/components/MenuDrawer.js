import '../styles/components/MenuDrawer.css'
import React from 'react'

const MenuDrawer = props => {
    return (
        <div className={`menu-drawer ${props.className}`}>

        </div>
    )
}

export default MenuDrawer
